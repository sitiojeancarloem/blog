---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset
  xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">

  {%- comment -%}
  Filtra apenas o que é realmente público:
  - Páginas com output (page.output == true)
  - Não estão em drafts
  - Não estão em pastas internas
  - sitemap != false
  {%- endcomment -%}

  {% assign all = site.pages | concat: site.posts %}
  {% assign public = all | where_exp: "item", "item.sitemap != false" %}
  {% assign filtered = "" | split: "" %}

  {% for item in public %}
    {% assign path = item.path %}
    {% unless path contains '_drafts/' or path contains '_rootfiles/' or path contains '_includes/' or path contains '_layouts/' or path contains '_sass/' %}
      {% assign filtered = filtered | push: item %}
    {% endunless %}
  {% endfor %}

  {% for item in filtered %}
  <url>
    <loc>{{ site.url }}{{ item.url | replace:'index.html','' }}</loc>
    <lastmod>{{ item.date | date_to_xmlschema | default: site.time | date_to_xmlschema }}</lastmod>
    <changefreq>{% if item.url == '/' %}daily{% else %}weekly{% endif %}</changefreq>
    <priority>{% if item.url == '/' %}1.0{% else %}0.7{% endif %}</priority>
  </url>
  {% endfor %}
</urlset>
